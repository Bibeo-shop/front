import { EqualIcon } from '@/components/atom/svg'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof EqualIcon> = {
  title: 'Components/atom/svg/EqualIcon',
  component: EqualIcon,
  tags: ['autodocs'],
}

export default meta

type Story = StoryObj<typeof EqualIcon>

export const Default: Story = {}
